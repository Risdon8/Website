function form(number) {
    const form = number + "%";
}
form();

function multiply(arr) {
    const arr[1]*3
}
multiply[0, 3, 1];

let sum = 0;
function addFive(num) {
    sum = sum + num;
}
addFive(5);


function (countdown) {
    for (let i = 0; i < countdown.length; i++) {
        console.log(i)
} 
}
countdown(i);